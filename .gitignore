# dependencies
node_modules/

# build output
frontend/dist/

# PocketBase data (user data — never commit)
pb_data/
backend/pb_data/
*.db
*.db-shm
*.db-wal

# local env & secrets
.env
.env.*
!.env.example

# test artifacts
/tmp/
coverage/
*.log

# OS junk
.DS_Store

# TypeScript incremental build info (regenerated on every build)
frontend/tsconfig.tsbuildinfo

# git worktrees created for isolated feature work
.claude/worktrees/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# foodpack raw dataset downloads (hundreds of MB, never committed)
backend/work/
